import React from 'react';

// importing styles
import {HeaderContainer} from './HeaderStyles';

function Header(){
    return(
        <HeaderContainer>
            Header
        </HeaderContainer>
    );
}

export default Header;